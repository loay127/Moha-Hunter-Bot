name: ULP_FINAL_FIX
on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'
      - name: Everything_In_One
        run: |
          pip install telethon pyTelegramBotAPI
          python main.py
