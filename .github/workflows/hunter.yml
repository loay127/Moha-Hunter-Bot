name: ULP Monster Final
on:
  workflow_dispatch:

jobs:
  The-Monster:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Setup & Run
        run: |
          pip install telethon pyTelegramBotAPI
          python bot.py & sleep 15 && python main.py
