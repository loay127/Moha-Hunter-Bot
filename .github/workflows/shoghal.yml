name: SHOGHAL_ULP
on:
  workflow_dispatch:

jobs:
  run_everything:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Start
        run: |
          pip install telethon pyTelegramBotAPI
          python main.py
